# 선비북스 전자책 대시보드 디자인 시스템

## 1. 제품 성격

- 출판 운영자가 제작·검수·유통·정산 상태를 빠르게 판단하는 업무용 대시보드다.
- 장식보다 정보 위계, 수치 가독성, 상태 구분, 다음 행동의 명확성을 우선한다.

## 2. 시각 방향

- 밝은 회백색 작업면과 짙은 숲색 사이드바를 기본 골격으로 사용한다.
- 브랜드 포인트는 라임색이며 성공은 초록, 정보는 파랑, 주의는 호박색으로 구분한다.
- 카드 모서리는 작고 절제하며, 과도한 그라데이션과 유리 효과는 사용하지 않는다.

## 3. 색상 토큰

- 본문 `#17201c`, 보조문자 `#69736e`, 경계선 `#dfe3de`
- 작업면 `#f5f6f3`, 카드 `#ffffff`, 브랜드 숲색 `#17251f`
- 포인트 라임 `#e8ff71`, 성공 `#178653`, 정보 `#2465c7`, 주의 `#a46410`
- 데이터 시각화는 위 토큰을 중심으로 채도와 명도를 조절한다.

## 4. 타이포그래피

- Pretendard, Apple SD Gothic Neo, Inter, 시스템 산세리프 순으로 사용한다.
- 페이지 제목은 굵고 촘촘한 자간, 본문과 설명은 충분한 행간을 유지한다.
- 금액과 ISBN처럼 비교가 필요한 숫자는 tabular numeral을 사용한다.

## 5. 레이아웃

- 데스크톱은 232px 고정 사이드바와 유동형 본문으로 구성한다.
- 본문 최대 폭은 1640px이며 바깥 여백은 화면 크기에 따라 유동적으로 조절한다.
- 모바일에서는 사이드바를 숨기고 68px 상단 헤더와 단일 열 콘텐츠를 사용한다.

## 6. 컴포넌트

- KPI 카드는 라벨, 핵심 수치, 보조 설명의 3단 위계를 유지한다.
- 운영 카드와 표는 1px 경계선과 흰 배경을 기본으로 하고 상태색은 필요한 곳에만 쓴다.
- 버튼은 기본 42px 높이, 작은 버튼은 34px 높이를 유지한다.
- 그래프는 동일 데이터를 표로도 제공하고 추정·누락 상태를 별도로 표시한다.
- `수정 요청 작성기`는 Codex 요청창처럼 문맥 제목, 큰 요청 입력란, 이미지 첨부 트레이, 하단 도구막대와 전송 버튼을 하나의 작업면에 배치한다.
- `대량 작업 업로더`는 파일 선택, 행 검증 요약, 미리보기 표, 정상 행 등록 버튼의 4단 구조를 사용한다.
- `제작 기획 승인 카드`는 선택 체크박스, 제목·약속, 독자·유형·권장 분량·차별화, 제공 자산, 목차를 한 카드에서 확인하게 한다.
- 첨부 트레이는 비어 있음, 이미지 선택됨, 제한 초과, 전송 중 상태를 구분하고 이미지마다 미리보기·파일명·개별 삭제 동작을 제공한다.
- macOS 캡처 이미지는 수정 입력창에서 `⌘V`로 바로 붙여넣고, 파일은 선택하거나 작업면에 드래그앤드롭할 수 있어야 한다.
- 수정 요청 이미지는 한 번에 최대 5장, JPG·PNG·WebP만 허용하며 파일 제한과 Codex 전달 방식을 입력창 가까이에 안내한다.

## 7. 상호작용과 접근성

- 링크·버튼은 키보드 포커스와 충분한 클릭 영역을 제공한다.
- 색상만으로 상태를 전달하지 않고 텍스트 라벨을 함께 표시한다.
- 데이터 자동 갱신은 현재 기준 시각과 원천 데이터 일자를 화면에 노출한다.

## 8. 반응형 원칙

- 1280px 이하에서 넓은 그리드를 줄이고, 1080px 이하에서 KPI를 2열로 바꾼다.
- 760px 이하에서는 모든 주요 카드와 표 제어를 단일 열로 배치한다.
- 가로 스크롤이 필요한 표는 독립 스크롤 영역으로 제한한다.
